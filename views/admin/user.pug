extends ./blocks/nav.pug
block content
  .app-content
    #main
      .app-title
        ul.app-breadcrumb.breadcrumb
          li.breadcrumb-item  Danh s&aacute;ch
          #time.ontime
      // <div class="row">
      .tile
        h3.tile-title Danh s&aacute;ch kh&aacute;ch h&agrave;ng
        table.table.table-bordered.table-hover
          thead.thead-light
            tr
              th(scope='col')
                .form-check
                  input#flexCheckDefault.form-check-input(type='checkbox', value='')
                  label.form-check-label(for='flexCheckDefault')
              th(scope='col') T&ecirc;n kh&aacute;ch h&agrave;ng
              th(scope='col') S&#x1ED1; &dstrok;i&#x1EC7;n tho&#x1EA1;i
              th(scope='col') Email
              th(scope='col') T&iacute;nh n&abreve;ng
          tbody
            each user in users
              tr
                td(scope='row') #{user.id}
                td #{user.name}
                td #{user.number}
                td #{user.email}
                td.iconss
                  i#btn-delete.fas.fa-trash-alt.icon--delete.backgrd
                  a(href='add-customer.html')
                    i.fas.fa-edit.backgrds
      // </div>
